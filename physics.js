"use strict";
/* =========================================================================
   Настройки физики — единственное место, где живут числа.

   Менять можно двумя способами:
     1) прямо здесь руками — это просто числа, математики тут нет;
     2) ползунками в игре (⚙ → «Физика»), а потом нажать «Экспорт» —
        кнопка отдаёт готовое содержимое ЭТОГО файла, останется заменить.

   Значения отсюда — общие для всех запусков и всех игроков. Ползунки в игре
   переопределяют их только в текущем браузере и только те параметры, которые
   реально двигали: всё остальное продолжает браться отсюда.
   ========================================================================= */
window.PHYSICS = {
  // Бег
  SPEED: 106,             // базовая скорость бега, мировых единиц в секунду
  SPRINT: 150,            // скорость со спринтом
  ACCEL: 1700,            // ускорение: чем больше, тем резче отклик управления
  GK_SPEED: 94,           // скорость вратаря

  // Мяч
  GRAV: 680,              // гравитация: меньше — мяч дольше висит в воздухе
  BOUNCE: 0.5,            // доля скорости, сохраняемая при отскоке от газона
  GROUND_FRICTION: 1.05,  // затухание качения по газону
  AIR_FRICTION: 0.14,     // затухание в полёте

  // Пас низом
  PASS_MIN: 300,          // сила при коротком тапе
  PASS_MAX: 820,          // сила при полном заряде

  // Удар по воротам
  SHOT_MIN: 480,
  SHOT_MAX: 940,
  SHOT_MIN_LOFT: 20,      // подъём растёт как квадрат заряда:
  SHOT_MAX_LOFT: 470,     // «свеча» только у самого максимума

  // Навес
  LOB_MIN_SPEED: 240,
  LOB_MAX_SPEED: 700,
  LOB_MIN_LOFT: 190,
  LOB_MAX_LOFT: 560,

  // Шкала усилия
  CHARGE_TIME: 0.8,       // секунд до полного заряда
  CHARGE_MIN: 0.32,       // доля силы при мгновенном тапе

  // Борьба за мяч
  CTRL_R: 27,             // радиус подбора свободного мяча
  TACKLE_R: 28,           // радиус автоматического отбора у ИИ
  TACKLE_STEAL_R: 46,     // радиус отбора по кнопке «Пас»
  STEAL_RATE: 2.2,        // попыток отбора в секунду у ИИ
  DRIBBLE_AHEAD: 24,      // насколько мяч выносится вперёд при ведении
};
