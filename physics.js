"use strict";
/* =========================================================================
   Настройки физики — единственное место, где живут числа.

   Менять можно двумя способами:
     1) прямо здесь руками — это просто числа, математики тут нет;
     2) ползунками в игре (⚙ → «Физика»), а потом нажать «Экспорт» —
        кнопка отдаёт готовое содержимое ЭТОГО файла, останется заменить.

   Значения отсюда — общие для всех запусков и всех игроков. Ползунки в игре
   переопределяют их только в текущем браузере и только те параметры, которые
   реально двигали: всё остальное продолжает браться отсюда.
   ========================================================================= */
window.PHYSICS = {
  // Бег
  SPEED: 148,               // Скорость бега
  SPRINT: 202,              // Спринт
  ACCEL: 2350,              // Ускорение (отклик)
  GK_SPEED: 162,            // Скорость вратаря

  // Мяч
  GRAV: 540,                // Гравитация
  BOUNCE: 0.6,              // Отскок от газона
  GROUND_FRICTION: 1.15,    // Трение о газон
  AIR_FRICTION: 0.1,        // Сопротивление воздуха

  // Пас
  PASS_MIN: 300,            // Сила: короткий тап
  PASS_MAX: 820,            // Сила: полный заряд

  // Удар
  SHOT_MIN: 480,            // Сила: минимум
  SHOT_MAX: 940,            // Сила: максимум
  SHOT_MIN_LOFT: 80,        // Подъём: минимум
  SHOT_MAX_LOFT: 470,       // Подъём: максимум

  // Навес
  LOB_MIN_SPEED: 240,       // Скорость: минимум
  LOB_MAX_SPEED: 700,       // Скорость: максимум
  LOB_MIN_LOFT: 50,         // Дуга: минимум
  LOB_MAX_LOFT: 1000,       // Дуга: максимум

  // Шкала усилия
  CHARGE_TIME: 0.8,         // Время до максимума, с
  CHARGE_MIN: 0.32,         // Доля силы при тапе

  // Борьба за мяч
  CTRL_R: 27,               // Радиус подбора
  TACKLE_STEAL_R: 40,       // Радиус моего отбора
  TACKLE_R: 20,             // Радиус отбора ИИ
  STEAL_RATE: 5.7,          // Частота отбора ИИ, 1/с
  DRIBBLE_AHEAD: 24,        // Вынос мяча при ведении
};
